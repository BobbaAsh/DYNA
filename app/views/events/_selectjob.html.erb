<button type="button" class="btn5 btn-info" data-toggle="modal" data-target="#exampleModal">
  Candidater
</button>
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Candidature au <%= @event.name %></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
          <%= simple_form_for ([locals[:event], locals[:model]]) do |f| %>
          <%= f.input :wanted_status, label: "Poste souhaité", collection: ["Entrée" , "Entretien", "Prospectus", "Driver"] %>
          <%= f.button :submit, new_event_participation_path(locals[:event]), value: "Candidate", class: "btn2 btn-info"  %>
      <% end %>
      </div>
    </div>
  </div>
</div>
