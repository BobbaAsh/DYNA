
<div class="container pad20" style="width:60%; margin-top: 30px; margin-bottom: 30px;" >
  <div class="row">
    <div class="col-lg-12 ">
      <h1>User <%= @user.id %></h1>
      <%= cl_image_tag @user.photo, height: 300, width: 400, crop: :fill %>
      <p class="text-center"> <%= "last name : #{@user.last_name}" %> </p>
      <p class="text-center"> <%= "First name : #{@user.first_name}" %> </p>
      <p class="text-center"> <%= "Date of birth : #{@user.date_of_birth}" %> </p>
      <p class="text-center"> <%= "Addresse : #{@user.address}" %> </p>
      <p class="text-center"> <%= "Numeros de securité sociale : #{@user.ss_number}" %></p>
      <p class="text-center"> <%= "Numeros de telephone : #{@user.phone_number}" %> </p>
    </div>

     <p> <a href="./" class="btn btn-sucess">Index</a> </p>
     <p> <%= link_to('Logout', destroy_user_session_path, method: :delete) %> </p>
     <p> <%= link_to 'edit', edit_user_path, method: :get %> </p>
  </div>
</div>
